function initFooter(module) {
	throw new Error("The footer module failed to initialize");
	// console.log("footer initialized", module);
	// module.append("footer initialized");
}

export const init = initFooter;
